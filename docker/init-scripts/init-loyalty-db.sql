-- Loyalty Service Database Initialization
-- This database stores customer loyalty and rewards data

-- The database is automatically created by the POSTGRES_DB environment variable
-- Additional initialization can be added here if needed

-- Create indexes for better query performance
-- CREATE INDEX IF NOT EXISTS idx_customers_email ON customers(email);
-- CREATE INDEX IF NOT EXISTS idx_customers_tier ON customers(loyalty_tier);
-- CREATE INDEX IF NOT EXISTS idx_purchase_history_customer ON purchase_history(customer_id);
